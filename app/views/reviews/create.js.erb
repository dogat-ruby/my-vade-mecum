// confirm file called
console.log("create.js.erb file");
<% unless @has_error %>
  $("#review_text").val("");

  // # add new comment to end of comments section
  $("#reviews").prepend("<%=j render @review %>");

  // remove current DOM element stating there are no comments yet (if no comments yet exist)
  $("#reviews h4").html("");
<% end %>

// # replace review form so user can only comment once (if desirable)
// $(".reviews").html("<h1>You have reviewd on this book</h1>");
