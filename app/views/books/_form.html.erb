<%= form_for(@book, multipart: true) do |f| %>
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bk_title %><br>
    <%= f.text_field :bk_title %>
  </div>
  <div class="field">
    <%= f.label :bk_isbn %><br>
    <%= f.number_field :bk_isbn %>
  </div>
  <div class="field">
    <%= f.label :bk_author %><br>
    <%= f.text_field :bk_author %>
  </div>
  <div>
    <%= f.file_field :picture %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
